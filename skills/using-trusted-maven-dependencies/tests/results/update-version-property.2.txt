# skill_digest: ac3227fe0b42d9e153410755f6edf0fb372192a2d8f4f46d728f02750f919a08
There's no pom.xml in this directory. Let me create a demonstration of what you should update. Based on your scenario, here's what you need to change:

**Update the `<junit.version>` property in your pom.xml:**

```xml
<!-- Before -->
<properties>
  <junit.version>5.8.2</junit.version>
</properties>

<!-- After -->
<properties>
  <junit.version>5.8.2-redhat-00002</junit.version>
</properties>
```

**Ensure your pom.xml has the redhat-ga repository configured** (add to `<repositories>` and `<pluginRepositories>` if not already present):

```xml
<!-- In <repositories> -->
<repository>
  <id>redhat-ga</id>
  <url>https://maven.repository.redhat.com/ga</url>
  <releases>
    <enabled>true</enabled>
  </releases>
  <snapshots>
    <enabled>false</enabled>
  </snapshots>
</repository>

<!-- In <pluginRepositories> -->
<pluginRepository>
  <id>redhat-ga</id>
  <url>https://maven.repository.redhat.com/ga</url>
  <releases>
    <enabled>true</enabled>
  </releases>
  <snapshots>
    <enabled>false</enabled>
  </snapshots>
</pluginRepository>
```

The key point: **Replace the version property value** from `5.8.2` to `5.8.2-redhat-00002` to use the Red Hat trusted rebuild while maintaining the same semantic version (5.8.2).
