# skill_digest: 2e6cf11f3a656f698e71d6d762603d30cd68ab6ef5b208accd8739e6a495de6a
Without access to RedHat's package metadata or release notes, the general guidance is: **use the latest version, which would be `31.1-jre-redhat-00002`**.

The `-redhat-00001` → `-redhat-00002` progression indicates sequential rebuilds, usually for security patches, bug fixes, or compliance updates. RedHat increments the suffix when they repackage a library with improvements. Unless you have a specific reason to avoid `-redhat-00002` (like a known regression), it's the safer choice.

To be certain, you could:
1. **Check RedHat's package repositories** or release notes for that specific Guava version
2. **Look at the build timestamps** — the newer rebuild should be more recent
3. **Check your organization's security guidance** — if you have a security team managing trusted repository rebuilds, they may have documentation on these specific versions

If you're setting up your Maven project to use this trusted repository, I can help with that configuration using the `using-trusted-maven-dependencies` skill. Let me know if you'd like to do that.
