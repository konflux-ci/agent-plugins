# skill_digest: 2e6cf11f3a656f698e71d6d762603d30cd68ab6ef5b208accd8739e6a495de6a
You should update the `junit.version` property value from `5.8.2` to `5.8.2-redhat-00002`:

```xml
<junit.version>5.8.2-redhat-00002</junit.version>
```

The version property already uses variable substitution (`${junit.version}`), so updating just this property will apply the change to all dependencies using it.

You'll also want to verify that the `redhat-ga` repository is properly configured in your pom.xml or settings.xml so Maven can resolve this version from that trusted source instead of Maven Central.

Would you like me to help you make these changes, or do you need to configure the repository reference as well?
