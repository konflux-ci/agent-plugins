# skill_digest: 4d7c5bc939f867c7b1bdefccdca8cc9b237f2458d161240872fcbaebc2a669e5
I see. The Explore agent needs access to the Konflux repositories. Since I don't have direct access to them in this environment, let me provide guidance based on the architecture reference information:

## How to Find This Information

Based on the Konflux architecture guidance, here's how to find the current integration test configuration workflow:

### 1. **Integration Service Documentation**
Start with the `konflux-ci/architecture` repository:
- Read `architecture/core/integration-service.md` - This defines the service's responsibilities, CRDs it owns, and how it interacts with other services
- Check `diagrams/` for visual flows showing how integration tests fit into the overall pipeline

### 2. **User-Facing Configuration Guide**
From the `konflux-ci/docs` repository:
- Look in `modules/testing/` - This should contain:
  - `pages/` with user guides on configuring integration tests
  - Configuration examples and best practices
  - Workflow documentation for how users define and run tests

### 3. **Design Decisions**
Check `ADR/` in the architecture repo for decisions about integration test configuration:
- Search for ADRs mentioning "integration" or "test" to understand why the current design was chosen
- These will explain the architectural constraints and rationale

### 4. **CRD Specifications**
Look for Kubernetes Custom Resource Definitions in:
- Architecture service specs (how integration tests are defined as resources)
- Reference documentation in the docs repo for API details

## What to Look For

The current workflow likely includes:
- **How users define integration tests** (CRD format, configuration files)
- **Integration points** (where tests hook into the build/release pipeline)
- **Test execution model** (which components orchestrate test runs)
- **Result reporting** (how test results are captured and surfaced)

Would you like me to help if you can provide:
1. A path to local Konflux repositories, or
2. Permission to access the GitHub documentation?

This will allow me to pull the specific configuration details and propose informed architectural changes.
