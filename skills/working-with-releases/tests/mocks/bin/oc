#!/usr/bin/env bash
# Mock oc for testing working-with-releases skill

case "$*" in
    *"whoami -t"*)
        echo "sha256~mock-test-token-for-kubearchive"
        ;;
    *)
        echo "mock-oc: unhandled args: $*" >&2
        exit 1
        ;;
esac
